{
  "schemaVersion": 1,
  "id": "io.github.grootaiinfinity.hwmon",
  "name": "Hardware Monitor",
  "version": "1.5.1",
  "author": "groot",
  "license": "MIT",
  "description": "Live CPU / temperature readout in the bar; click for a detailed system panel (CPU cores, memory, thermals, GPUs, disks, network, top processes, battery).",
  "repository": "https://github.com/GrootAiInfinity/omarchy-hwmon",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "hwmon.qml"
  },
  "barWidget": {
    "displayName": "Hardware Monitor",
    "description": "CPU% + temperature in the bar. Right-click / scroll expands it to add memory, GPU and battery; left-click opens the full system panel. Reads kernel sysfs/procfs only - no elevated privileges, no network access.",
    "category": "System",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "expanded": false,
      "cpuView": "cores"
    },
    "schema": [
      {
        "key": "expanded",
        "type": "boolean",
        "label": "Start expanded",
        "defaultValue": false,
        "description": "Show the memory / GPU / battery readouts in the bar too (also toggled at runtime with right-click or scroll)."
      },
      {
        "key": "cpuView",
        "type": "enum",
        "label": "CPU grid",
        "options": [
          "cores",
          "threads"
        ],
        "defaultValue": "cores",
        "description": "Whether the grid under the CPU meter shows one block per physical core or one per thread (also switched in the panel itself)."
      }
    ]
  }
}
